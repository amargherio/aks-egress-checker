{
    "enabled": false,
    "rules": [
        {
            "name": "secrets-store-vault-access",
            "dst": "vault.azure.net",
            "protocol": "https",
            "port": "443",
            "use": "Required for CSI Secret Store addon pods to talk to Azure Key Vault servers.",
            "requiredPrivate": true,
            "enabled": true
        }
    ]
}